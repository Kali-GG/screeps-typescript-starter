import {missions} from "../missions/missions";

const miner = (creep: Creep, mission: MissionInfo) => {

  // check if in range of source
  //  check if fatigued
  //  move to designated spot if not in range
  // mine if there are still resources in the source
  // check if something needs to be build or repaired around
  // deposit if full & there is a not full link nearby

}

export { miner }
